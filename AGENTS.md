# Agent Rules

## Animation Policy (Mandatory)

- All animations must use Svelte-native methods (`svelte/transition`, `svelte/animate`, `svelte/motion`).
- Do not add new CSS-only animation behavior for interactive state changes.
- Do not add manual JavaScript animation loops for UI motion.
