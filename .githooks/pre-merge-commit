#!/usr/bin/env bash
set -euo pipefail

current_branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$current_branch" = "main" ]; then
	echo "Merge blocked: direct merge commits into 'main' are disabled in this repo."
	echo "Use branch merges through PR flow instead."
	exit 1
fi
