#!/usr/bin/env bash
set -euo pipefail

current_branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$current_branch" = "main" ]; then
	echo "Push blocked: direct pushes from 'main' are disabled in this repo."
	echo "Use a branch + PR workflow instead."
	exit 1
fi
