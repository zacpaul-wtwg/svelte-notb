[build]
  command = "rm -rf .netlify/functions-internal && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20.19.0"
  NPM_CONFIG_ENGINE_STRICT = "false"
  PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD = "1"

[functions]
  node_bundler = "esbuild"
  directory = "netlify/functions"


